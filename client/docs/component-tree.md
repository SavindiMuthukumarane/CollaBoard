# M1 Component Tree

```text
App
├── Navbar
└── Board
    ├── Column (To Do)
    │   └── TaskCard × n
    ├── Column (Doing)
    │   └── TaskCard × n
    └── Column (Done)
        └── TaskCard × n
```

Open `component-tree.svg` for the visual diagram.
